import { BadgeCheck, FileWarning, LogOut, ShieldCheck } from 'lucide-react';
import { requireAdmin } from '@/lib/admin-auth';
import { db } from '@/lib/db';
import { adminLogoutAction,adminReviewVerificationAction,adminUpdateClaimAction } from '@/app/actions';
import { statusLabel } from '@/lib/format';
export default async function Admin(){await requireAdmin();const verifications=db.prepare(`SELECT v.*,u.email,u.first_name,u.last_name,p.business_name,p.trades,p.postcode FROM verification_requests v JOIN users u ON u.id=v.provider_id JOIN provider_profiles p ON p.user_id=v.provider_id ORDER BY CASE v.status WHEN 'pending' THEN 0 ELSE 1 END,v.submitted_at DESC`).all() as any[];const claims=db.prepare(`SELECT c.*,j.title,hu.email homeowner_email,pu.email provider_email,p.business_name FROM claims c JOIN jobs j ON j.id=c.job_id JOIN users hu ON hu.id=c.homeowner_id JOIN users pu ON pu.id=c.provider_id JOIN provider_profiles p ON p.user_id=c.provider_id ORDER BY CASE c.status WHEN 'pending' THEN 0 WHEN 'reviewing' THEN 1 ELSE 2 END,c.updated_at DESC`).all() as any[];return <main className="admin-page"><header className="admin-header"><div><h1>Mein Hausmeister · Admin</h1><p>Partnerprüfung, Qualität und Garantie-/Problemfälle.</p></div><form action={adminLogoutAction}><button className="btn ghost"><LogOut size={16}/>Abmelden</button></form></header><div className="admin-grid"><section className="admin-panel"><h2><BadgeCheck/> Partnerprüfungen</h2>{verifications.length===0&&<p className="muted">Keine Prüfungen vorhanden.</p>}<div className="stack">{verifications.map(v=><article className="admin-card" key={v.id}><div className="admin-card-head"><div><strong>{v.business_name}</strong><small>{v.first_name} {v.last_name} · {v.email}<br/>{v.trades} · {v.postcode}</small></div><span className={`status ${v.status}`}>{statusLabel(v.status)}</span></div><a className="admin-file" href={`/api/admin/verification-file/${v.id}`} target="_blank" rel="noreferrer">Prüfdokument öffnen</a>{v.provider_note&&<p>{v.provider_note}</p>}<form action={adminReviewVerificationAction.bind(null,v.id)} className="admin-form"><textarea name="adminNote" rows={2} defaultValue={v.admin_note||''} placeholder="Interne/Partner-Notiz"/><div className="action-row"><button name="decision" value="approved" className="btn primary"><ShieldCheck size={16}/>Freigeben</button><button name="decision" value="rejected" className="btn ghost">Ablehnen</button></div></form></article>)}</div></section><section className="admin-panel"><h2><FileWarning/> Problemfälle</h2>{claims.length===0&&<p className="muted">Keine Problemfälle vorhanden.</p>}<div className="stack">{claims.map(c=><article className="admin-card" key={c.id}><div className="admin-card-head"><div><strong>{c.title}</strong><small>Kunde: {c.homeowner_email}<br/>Partner: {c.business_name} · {c.provider_email}</small></div><span className={`status ${c.status}`}>{statusLabel(c.status)}</span></div><p>{c.description}</p><form action={adminUpdateClaimAction.bind(null,c.id)} className="admin-form"><label>Status<select name="status" defaultValue={c.status}><option value="pending">Offen</option><option value="reviewing">In Prüfung</option><option value="resolved">Gelöst</option><option value="rejected">Abgelehnt</option></select></label><textarea name="adminNote" rows={3} defaultValue={c.admin_note||''} placeholder="Rückmeldung / Entscheidung"/><button className="btn primary">Fall aktualisieren</button></form></article>)}</div></section></div></main>}
