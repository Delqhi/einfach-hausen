import Link from 'next/link'; import { Logo } from '@/components/logo'; import { loginAction } from '@/app/actions';
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;return <main className="auth-page"><form action={loginAction} className="auth-card"><Logo/><h1>Willkommen zurück</h1><p>Einloggen und direkt weitermachen.</p>{sp.error&&<div className="alert error">{sp.error}</div>}<label>E-Mail<input name="email" type="email" required autoComplete="email"/></label><label>Passwort<input name="password" type="password" required autoComplete="current-password"/></label><button className="btn primary wide">Einloggen</button><small>Noch kein Konto? <Link href="/register">Jetzt registrieren</Link></small></form></main>}
