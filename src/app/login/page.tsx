import Link from 'next/link'; import { Logo } from '@/components/logo'; import { loginAction } from '@/app/actions';
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;return <main className="auth-page auth-reference"><form action={loginAction} className="auth-card auth-reference-card"><Logo/><div className="auth-reference-heading"><span>Eigenheim</span><h1>Willkommen zurück</h1><p>Einloggen und direkt weitermachen.</p></div>{sp.error&&<div className="alert error" role="alert">{sp.error}</div>}<label>E-Mail<input name="email" type="email" required autoComplete="email"/></label><label>Passwort<input name="password" type="password" required autoComplete="current-password"/></label><button className="btn primary wide">Einloggen</button><small className="auth-reference-foot">Noch kein Konto? <Link href="/register">Jetzt registrieren</Link></small></form></main>}
