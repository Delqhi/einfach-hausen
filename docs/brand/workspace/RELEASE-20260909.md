# Workspace release 2026-09-09

Jerry authorized integration and release with “Erledige du das”.

## Evidence

- Integrated origin/main 90f94bd without dropping agent changes.
- Full isolated release-gate at b196f17: 15/15 passed; log /tmp/eh-workspace-gate.log.
- GitHub Actions did not start: account locked due to billing (check-run annotation 102314282580). No check result was fabricated.
- Removed pre-existing internal-doc inline styling and foreign color fallbacks; now consumes EHScope/EHSection. Guard without base and all nine guard tests pass. Debt baseline unchanged.
- Trusted-base guard requires brand authority for the intentional canonical package changes; Jerry explicitly commissioned these design changes. Ordinary agents must not bypass this rule.
- Deployment must run the complete standard release gate again after this documentation/style fix.

## Scope and remaining work

The new owner/provider shell and composed overview/list/calendar/profile/conversation routes are integrated with their existing data/actions. EHJobDetail, EHAttachmentPanel and EHInvoiceEditor are reusable design-preview components only; their presence in the repository does not mean the live detail/upload/invoice workflows have been migrated. Existing production invoice form remains in use. Do not remove existing authorization, upload validation, payment or status actions when wiring these components.

NEXT_AGENT: migrate actual detail route compositions, bind attachment selection to existing validated upload action, bind invoice editor to existing createInvoiceAction preserving redirect and error behavior; verify authenticated desktop/mobile screens and real workflow states before claiming professional-product completion. No final visual acceptance is implied by a green release gate.
